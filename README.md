# Portfolio Website - เว็บไซต์ Portfolio

เว็บไซต์ Portfolio สำหรับแสดงผลงานและแนะนำตัวในสาขาคอมพิวเตอร์

## คุณสมบัติ

- 🎨 **ดีไซน์ทันสมัย** - UI/UX ที่สวยงามและใช้งานง่าย
- 📱 **Responsive Design** - รองรับการแสดงผลบนทุกอุปกรณ์
- ⚡ **Performance ดี** - โหลดเร็วและลื่นไหล
- 🎭 **Animations** - มี animations ที่น่าสนใจ
- 🌐 **รองรับภาษาไทย** - ใช้ฟอนต์ Sarabun สำหรับภาษาไทย

## ส่วนประกอบของเว็บไซต์

1. **หน้าแรก (Hero Section)** - แนะนำตัวและข้อความต้อนรับ
2. **เกี่ยวกับฉัน (About)** - ข้อมูลส่วนตัวและสถิติ
3. **ทักษะ (Skills)** - แสดงทักษะและเทคโนโลยีที่ใช้ได้
4. **ผลงาน (Projects)** - แสดงโปรเจกต์ที่เคยทำ
5. **ติดต่อ (Contact)** - ฟอร์มติดต่อและข้อมูลการติดต่อ

## การใช้งาน

### วิธีเปิดใช้งาน

1. เปิดไฟล์ `index.html` ในเบราว์เซอร์
2. หรือใช้ Live Server ใน VS Code:
   - ติดตั้ง extension "Live Server"
   - คลิกขวาที่ `index.html` และเลือก "Open with Live Server"

### การแก้ไขเนื้อหา

> **หมายเหตุสำคัญ:** เว็บไซต์นี้ใช้ระบบ **Config-based** ซึ่งหมายความว่าคุณสามารถแก้ไขข้อมูลทั้งหมดได้ในไฟล์ `config.js` โดยไม่ต้องแก้ไข HTML โดยตรง!

#### แก้ไขข้อมูลทั้งหมดใน config.js

เปิดไฟล์ `config.js` และแก้ไขข้อมูลตามต้องการ:

##### 1. ข้อมูลทั่วไป (Site Info)
```javascript
site: {
    title: "Portfolio - ผลงานคอมพิวเตอร์",
    brand: "Portfolio"
}
```

##### 2. ข้อมูลส่วนตัว - Hero Section
```javascript
hero: {
    greeting: "สวัสดีครับ",
    nameHighlight: "นักพัฒนา",
    description: "ที่หลงใหลในเทคโนโลยีและนวัตกรรม",
    subtitle: "ยินดีต้อนรับสู่ Portfolio ของฉัน",
    profileImage: "images/profile/profile.jpg", // ใส่ path หรือ null
    profileImageAlt: "โปรไฟล์"
}
```

##### 3. ข้อมูล About Section
```javascript
about: {
    title: "เกี่ยวกับฉัน",
    paragraphs: [
        "ข้อความย่อหน้าแรก",
        "ข้อความย่อหน้าที่สอง"
    ],
    stats: [
        {
            number: 10,
            label: "โปรเจกต์",
            suffix: "+"
        },
        // เพิ่ม stats อื่นๆ ได้
    ],
    aboutImage: "images/profile/about.jpg" // หรือ null
}
```

##### 4. ทักษะ (Skills)
```javascript
skills: {
    items: [
        {
            icon: "fab fa-html5",      // Font Awesome icon class
            name: "Frontend",
            description: "HTML, CSS, JavaScript, React, Vue.js",
            level: 85                  // เปอร์เซ็นต์ (0-100)
        },
        // เพิ่มทักษะอื่นๆ ได้
    ]
}
```

##### 5. ผลงาน (Projects)
```javascript
projects: {
    items: [
        {
            title: "ชื่อโปรเจกต์",
            description: "คำอธิบายโปรเจกต์",
            image: "images/projects/project1.jpg", // หรือ null สำหรับ placeholder
            imageAlt: "ชื่อโปรเจกต์",
            placeholderIcon: "fas fa-laptop-code", // ใช้เมื่อ image เป็น null
            tags: ["React", "Node.js", "MongoDB"],
            links: {
                demo: "https://your-demo-link.com",    // ลิงก์ demo
                github: "https://github.com/..."      // ลิงก์ GitHub
            }
        },
        // เพิ่มโปรเจกต์อื่นๆ ได้
    ]
}
```

##### 6. ข้อมูลติดต่อ (Contact)
```javascript
contact: {
    description: "ข้อความอธิบายส่วนติดต่อ",
    methods: [
        {
            icon: "fas fa-envelope",
            type: "อีเมล",
            value: "your.email@example.com",
            link: "mailto:your.email@example.com"
        },
        // เพิ่มช่องทางติดต่ออื่นๆ ได้
    ]
}
```

##### 7. Social Media Links (Footer)
```javascript
social: {
    links: [
        {
            icon: "fab fa-github",
            url: "https://github.com/yourusername"
        },
        // เพิ่ม social links อื่นๆ ได้
    ],
    copyright: "© 2024 Portfolio. สร้างด้วย HTML, CSS และ JavaScript"
}
```

##### 8. Navigation Menu
```javascript
navigation: {
    items: [
        { label: "หน้าแรก", href: "#home" },
        { label: "เกี่ยวกับ", href: "#about" },
        // เพิ่มเมนูอื่นๆ ได้
    ]
}
```

#### ตัวอย่างการเพิ่มโปรเจกต์ใหม่

```javascript
{
    title: "โปรเจกต์ใหม่ของฉัน",
    description: "คำอธิบายโปรเจกต์นี้...",
    image: "images/projects/new-project.jpg",
    imageAlt: "โปรเจกต์ใหม่",
    placeholderIcon: "fas fa-rocket",
    tags: ["Vue.js", "Firebase", "PWA"],
    links: {
        demo: "https://myproject.com",
        github: "https://github.com/username/project"
    }
}
```

#### ตัวอย่างการเพิ่มทักษะใหม่

```javascript
{
    icon: "fab fa-python",
    name: "Python",
    description: "Python, Django, Flask, Data Science",
    level: 90
}
```

### การปรับแต่งสี

แก้ไขในไฟล์ `styles.css` ที่ส่วน `:root` (บรรทัด ~13-22):

```css
:root {
    --primary-color: #6366f1;      /* สีหลัก */
    --primary-dark: #4f46e5;       /* สีหลักเข้ม */
    --secondary-color: #8b5cf6;    /* สีรอง */
    --text-dark: #1e293b;          /* สีข้อความเข้ม */
    --text-light: #64748b;         /* สีข้อความอ่อน */
}
```

### เพิ่มรูปภาพ

#### โครงสร้างโฟลเดอร์รูปภาพ

เว็บไซต์มีโฟลเดอร์ `images/` พร้อม subfolders:
- `images/profile/` - สำหรับรูปภาพโปรไฟล์
- `images/projects/` - สำหรับรูปภาพผลงานโปรเจกต์

#### วิธีเพิ่มรูปภาพโปรไฟล์

1. เพิ่มรูปภาพในโฟลเดอร์ `images/profile/` (เช่น `profile.jpg`)
2. แก้ไขใน `config.js`:
   ```javascript
   hero: {
       profileImage: "images/profile/profile.jpg",
       // ...
   },
   about: {
       aboutImage: "images/profile/about.jpg",
       // ...
   }
   ```

#### วิธีเพิ่มรูปภาพผลงาน

1. เพิ่มรูปภาพในโฟลเดอร์ `images/projects/` (เช่น `webapp.jpg`)
2. แก้ไขใน `config.js` ในส่วน projects:
   ```javascript
   {
       title: "เว็บแอปพลิเคชัน",
       image: "images/projects/webapp.jpg",
       // ...
   }
   ```

**หมายเหตุ:** หากไม่ใส่ `image` หรือใส่เป็น `null` ระบบจะแสดง placeholder icon แทน

ดูคำแนะนำเพิ่มเติมได้ใน `images/README.md`

## โครงสร้างไฟล์

```
portfolio_website/
├── index.html          # โครงสร้าง HTML หลัก
├── styles.css          # ไฟล์ CSS สำหรับ styling
├── script.js           # JavaScript สำหรับ interactivity และ rendering
├── config.js           # ⭐ ไฟล์ config สำหรับจัดการข้อมูลทั้งหมด
├── README.md           # คู่มือการใช้งาน
└── images/            # โฟลเดอร์รูปภาพ
    ├── profile/        # รูปภาพโปรไฟล์
    ├── projects/      # รูปภาพผลงาน
    └── README.md      # คู่มือรูปภาพ
```

## เทคโนโลยีที่ใช้

- **HTML5** - โครงสร้างเว็บไซต์
- **CSS3** - การจัดรูปแบบและดีไซน์
- **JavaScript (Vanilla)** - เพิ่มความ interactive และ rendering จาก config
- **Config.js** - ระบบจัดการข้อมูลแบบ centralized
- **Font Awesome** - ไอคอน
- **Google Fonts (Sarabun)** - ฟอนต์ภาษาไทย

## ระบบ Config

เว็บไซต์ใช้ระบบ **Config-based Architecture** ซึ่งมีข้อดี:

- ✅ **แยกข้อมูลออกจากโครงสร้าง** - แก้ไขข้อมูลได้โดยไม่ต้องแตะ HTML
- ✅ **จัดการง่าย** - ข้อมูลทั้งหมดอยู่ที่เดียวใน `config.js`
- ✅ **ยืดหยุ่น** - เพิ่ม/ลบ/แก้ไขข้อมูลได้ง่าย
- ✅ **สะอาด** - HTML มีแค่โครงสร้าง ไม่มีข้อมูล hardcode

### ตัวอย่างการใช้งาน

แทนที่จะแก้ไข HTML แบบนี้:
```html
<h3 class="project-title">เว็บแอปพลิเคชัน</h3>
<p class="project-desc">คำอธิบาย...</p>
```

แค่แก้ไขใน `config.js`:
```javascript
projects: {
    items: [
        {
            title: "เว็บแอปพลิเคชัน",
            description: "คำอธิบาย...",
            // ...
        }
    ]
}
```

ระบบจะ render ข้อมูลจาก config อัตโนมัติเมื่อโหลดหน้าเว็บ!

## Browser Support

- Chrome (ล่าสุด)
- Firefox (ล่าสุด)
- Safari (ล่าสุด)
- Edge (ล่าสุด)

## หมายเหตุ

- ฟอร์มติดต่อในเว็บไซต์นี้เป็นเพียงตัวอย่าง ต้องเชื่อมต่อกับ backend หรือ email service เพื่อใช้งานจริง
- แนะนำให้เพิ่มรูปภาพจริงของคุณแทน placeholder
- สามารถปรับแต่งสี, ฟอนต์, และดีไซน์ตามต้องการ

## License

เว็บไซต์นี้เป็น open source และสามารถใช้ได้ฟรี

---

สร้างด้วย ❤️ สำหรับนักพัฒนาและนักเรียนคอมพิวเตอร์

